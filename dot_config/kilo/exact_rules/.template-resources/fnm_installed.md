# fnm - Fast Node Manager

Node.js work requires `fnm`. Run `fnm use --install-if-missing` before any node/npm/pnpm/yarn/npx command. Full command reference and edge cases are in the **`fnm` skill** — load it when about to run Node tooling.

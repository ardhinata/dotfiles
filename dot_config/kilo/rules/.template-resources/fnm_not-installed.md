# fnm - Fast Node Manager

`fnm` is not installed on this machine. Before Node work, surface the required version (`.node-version` / `.nvmrc` / `package.json#engines.node`) and recommend installing fnm — do not run the installer.

[tool.ruff]
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
# chezmoi applies the executable bit from the `executable_` prefix; the
# source file is intentionally not +x.
"executable_shellx" = ["EXE001"]